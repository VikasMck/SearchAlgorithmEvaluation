# AIP_TwoM1sAndADream

Aim:
- Create menu UI (done)
- Create animation (partly, needs each step shown + better colours)
- Implement algorithms + paradigms (foundation done)
- Allow for multiple attempts, and store statistics for comparison. (Difficult with matplot as seems to crash, needs a workaround)
- Add another menu item to check which algorithms ran and their statistics.
- Add an option to run all (Optional).
- Add an option to customise maps in the program (Optional).


Currently code from the labs is used as inspiration, later changes may be made for optimisation.
Naming convention fixed, unlike the lab code.


Using MacOSX, as this is done on M1 MacBooks.